<html lang="en">
<%- include('../partials/header.ejs') %> <!--Includes the HEAD element from the partials file-->
<body>
    <nav>                                <!--The NAV on the home page is not included in the partials folder, as the home page nav looks different from the other pages-->
        <div class="title-cards">
            <div id="AboutCard">
                <div class="Menu">
                    <h2>About Us</h2>
                    <button type="button"><a href="/about">View About</a></button>
                </div>
            </div>

            <div id="EventsCard">
                <div class="Menu">
                    <h2>Events</h2>
                    <button type="button"><a href="/events">View Events</a></button>
                </div>
            </div>

            <div id="ContactCard">
                <div class="Menu">
                    <h2>Contact Us</h2>
                    <button type="button"><a href="/contact">Get into Contact</a></button>
                </div>
            </div>
        </div>
    </nav>

    <h1 id="Welcome">Welcome to the Belgium Campus Community Portal</h1>

    <div id="upcoming-events">
    <% if (upcomingEvents.length > 0) { %>
        <h3>Upcoming Events:</h3>
        <div class="event-grid">
        <% upcomingEvents.forEach(event => { %>
            <div class="event-block">  <!-- Changed from event-card to match events page -->
            <% if (event.image) { %>
                <img src="<%= event.image %>" alt="<%= event.title %>" class="event-image">
            <% } %>
            <div class="event-details">
                <h3><%= event.title %></h3>
                <p class="event-date">📅 <%= new Date(event.date).toLocaleDateString() %></p>
                <p class="event-location">📍 <%= event.location %></p>
                <% if (event.description) { %>
                <p class="event-description"><%= event.description %></p>
                <% } %>
            </div>
            </div>
        <% }); %>
        </div>
    <% } else { %>
        <p class="no-events">No upcoming events scheduled. Check back later!</p>
    <% } %>
    </div>

    <footer>
        <%- include('../partials/footer.ejs') %>   <!--Includes the Footer element from the partials file-->
    </footer>
</body>
</html>